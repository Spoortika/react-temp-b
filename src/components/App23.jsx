import React from 'react'

const App23 = () => {
  return (
    <div>
        <p>
       <b> Default Props:</b>
       in ReactJS default props are used to define default values for props in a component.
       this is helpful when ypu want your component to have a fallback value if prop isnt  provied.
       </p>
    </div>
  )
}

export default App23